<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- CSS only -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor" crossorigin="anonymous">
    <title>Article</title>
    <style>
        body{
            background: url('/assets/bg4.png');
        }
        .my{
           
            margin:20px;
        }
        .title{
            text-align: center;
            font-size: 2.5rem;
            font-weight: bold;
        }
        .date{
            font-size: 1.5rem;
            text-align: center;
            font-weight: bold;
        }
        .desc{
            font-size: 1.5rem;
            text-align: center;
            font-weight: bold;
        }
        .bt{
            display: block;
            width: 5%;
            margin:20px auto;
            font-size: 1rem;
        }
    </style>
</head>
<body>
    <div class="container my">
       <h1 class="mb-1 title"><%= article.title %></h1>
       <div class="text-muted mb-2 date"><%= article.createdat.toLocaleDateString() %></div>
       <div class="desc">   
        <%= article.markdown %>
        </div>
       <a href="/job" class="btn btn-danger bt">Back</a> 
    </div>
</body>
</html>